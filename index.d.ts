declare function size(all?: boolean): number;
declare function doDefault(displayId: number): boolean;
declare function do90(displayId: number): boolean;
declare function do180(displayId: number): boolean;
declare function do270(displayId: number): boolean;
declare function clockwise(displayId: number): boolean;
declare function counterClockwise(displayId: number): boolean;

export = {
    size,
    doDefault,
    do90,
    do180,
    do270,
    clockwise,
    counterClockwise,
};
